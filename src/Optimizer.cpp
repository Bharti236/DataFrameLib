#include "../include/DataFrameLib/Optimizer.h"

// Step 5 implementation will live here.
//
// The file intentionally includes Optimizer.h already so the project build
// type-checks the full optimizer interface even before the rewrite engine is
// implemented.
